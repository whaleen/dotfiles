local colors = require("colors")

sbar.bar({
  position      = "top",
  height        = 24,
  color         = colors.bar.bg,
  border_color  = colors.bar.border,
  border_width  = 0,
  padding_left  = 4,
  padding_right = 4,
  blur_radius   = 0,
})
