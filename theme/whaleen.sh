#!/bin/bash
# Whaleen — palette shim (sources dark palette by default)
# To use the light palette: source theme/palettes/light.sh
# To generate both: ./theme/generate.sh --palette=all
source "$(dirname "$0")/palettes/dark.sh"
